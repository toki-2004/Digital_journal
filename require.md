请为我开发一个**自托管的实时协同电子手账**（全栈 Web 应用）。

**运行环境与启动方式：**
- 后端使用 Node.js + Express + Socket.io（WebSocket 实时通信）。
- 前端为单页应用（所有 HTML/CSS/JS 整合在单个 `index.html` 中）。
- 数据存储于本地 `./data/` 目录下的 JSON 文件（无需安装数据库）。
- 启动命令：`npm install express socket.io` 和 `node server.js`。
- 服务监听 `0.0.0.0:3000`（方便配合 ngrok 或 frp 进行端口转发，实现公网访问）。

**必须实现的核心功能清单：**

1. **多账本（房间）管理**
   - 首页展示所有手账本列表（含封面缩略图、名称、最后修改时间）。
   - 支持新建、重命名、删除手账本。每个手账本对应一个唯一的房间 ID，数据完全隔离。

2. **实时协同与权限控制**
   - 每个房间可设置“访问密码”和“编辑权限”（只读 / 可编辑）。创建者默认为管理员。
   - 基于 WebSocket 实现实时同步：当任意用户拖拽图片、绘制笔迹或添加图形时，所有在线成员画布自动刷新（无需手动刷新页面）。
   - 显示在线成员列表及各自光标位置（随机分配用户名和颜色，存储在 localStorage）。

3. **高自由度画布与工具**
   - **图层架构**：底层为“图片层”，上层为“标注层”。支持插入多张图片（点击上传或 Ctrl+V 粘贴剪贴板图片），图片可拖拽移动、缩放和旋转。
   - **绘图工具**：画笔（可调颜色/粗细）、橡皮擦（仅擦除标注层内容，不伤及图片）、几何图形（直线、矩形、圆形、带箭头的线段）、文字输入框（双击放置文本）。
   - **元素操作**：点击选中画布上的任意元素（图片或图形），支持删除、置顶/置底、复制粘贴。

4. **操作留痕与历史记录**
   - 右侧侧边栏以时间线形式记录每一步操作，格式示例：`[14:32:05] 用户A 在 (120, 45) 绘制了红色矩形`。
   - 操作者识别：基于浏览器生成的唯一用户 ID，所有广播消息携带操作者身份。

5. **自动保存与容灾恢复**
   - 每次操作完成后，后端立即将当前房间的全量数据序列化写入 `data/房间ID.json`。
   - 前端同时将数据缓存至 IndexedDB。服务重启或浏览器刷新后，自动从 JSON 文件恢复完整画布状态。

6. **导出功能**
   - 导出为高清 PNG 图片（支持透明/纯白背景切换）。
   - 导出为 PDF 文件（自动适配 A4 尺寸，分页展示长图内容）。

**技术实现约束（请严格遵守）：**
- 仅生成 **两个文件**：`server.js`（完整后端逻辑）和 `index.html`（包含全部前端样式、脚本和 DOM 结构）。
- 前端画布统一使用 Canvas 2D 渲染，不依赖第三方 UI 库。
- 通信协议：Socket.io 用于实时广播操作指令；RESTful API（Express）用于初次加载历史数据和获取房间列表。
- 图片压缩：上传的图片自动压缩至长边不超过 1600px，避免传输数据过大致使 JSON 文件膨胀。
- 冲突处理机制：同一时刻，同一元素被用户拖拽/编辑时，其他用户的光标变为锁定提示，释放后方可操作（简化的乐观锁策略）。

**项目交付要求：**
- 代码顶部必须包含详细的注释，说明依赖安装、启动流程以及如何通过端口转发（如 ngrok）对外分享。
- 确保中文字体显示正常，界面风格简约柔和（类似 Notion 或白板应用）。
- 所有功能须完整可运行，无报错，且数据持久化可靠。